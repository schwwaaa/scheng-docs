source "https://rubygems.org"

# Local build/runtime (your current setup)
gem "jekyll", "~> 4.3"
gem "just-the-docs", "~> 0.8.0"

# Required if `_config.yml` uses `remote_theme:` (GitHub Pages style)
gem "jekyll-remote-theme"

# Required for `jekyll serve` on Ruby 3+
gem "webrick", "~> 1.8"
