# ShadeCore SDK Overview

This document defines the **ShadeCore SDK surface**.

It describes what the engine guarantees, what is configurable, and what is explicitly *out of scope*. This is not a tutorial and does not reference specific examples.
